import { Stack } from "rsuite"

export default function NeedLogin() {
    return <Stack justifyContent="center" style={{marginTop: 50, fontSize: 18}}>
        <div>You need to login</div>
    </Stack>
}